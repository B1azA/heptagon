use super::core::*;

pub struct Loader {

}

impl Loader {
    pub fn new() -> Self {
        Self {}
    }

    // pub fn load_texture(path: &str) -> Result<Texture, String> {
    //     let bytes = std::fs::read(path).unwrap();
    //     // Texture::from_bytes(bundle, &bytes, label)
    // }
}